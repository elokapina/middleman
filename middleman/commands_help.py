COMMAND_WRITE = """Sends a message to a room using the bot. Usage:

`!message <room ID or alias> <Text to write>`

For example:

`!message #foobar:domain.tld Hello people in the Foobar room.`
"""
COMMAND_MWRITE = """Sends a messages to rooms using the bot. Usage:

`!massage <room ID or alias> <Text to write>`

For example:

`!massage #foobar:domain.tld Hello people in the Foobar room.`

"""
